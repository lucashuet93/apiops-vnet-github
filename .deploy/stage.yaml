---
ingress:
  hosts:
    - host: apim-be-stage.gc.bartbase.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: gc-bartbase
      hosts:
        - apim-be-stage.gc.bartbase.com

apimPublisher:
  enable: true
  jobSleep: 5
