---
ingress:
  hosts:
    - host: apim-be-test.gc.bartbase.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: gc-bartbase
      hosts:
        - apim-be-test.gc.bartbase.com

apimPublisher:
  enable: true
  jobSleep: 5
